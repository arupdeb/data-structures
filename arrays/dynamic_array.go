//static array
//len, cap
//create new array
//size, isEmpty
//get(index) return element //boubds check
//set(index,element) //bounds check
//clear, add
//removeAt(index), remove(object)
//indexof

package arrays

type Array struct {
	elements []interface{}
	size int
	capacity int
}

